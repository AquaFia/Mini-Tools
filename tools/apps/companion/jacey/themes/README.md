# Visual Context Themes

Phase 4A only creates the loader.

Optional files can later be added here using the exact visual-context key:

- `halloween.css`
- `birthday.css`
- `christmas.css`

A missing stylesheet is valid and is ignored automatically.

Theme CSS should override existing CSS variables/selectors only. The visual
context runtime does not replace the companion's base layout or identity theme.
